{
  "name": "simple-crud",
  "version": "1.0.0",
  "description": "nodejd server that communicates with a mysql database",
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "googleapis": "^59.0.0",
    "mysql": "^2.18.1",
    "npm-run-all": "^4.1.5",
    "react-stripe-checkout": "^2.6.3",
    "stripe": "^8.87.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "@heroku-postbuild": "cd frontend && npm install && npm run build",
    "build": "npm install && cd frontend && npm install && npm run build && cd .. && node index.js"
  },
  "keywords": [
    "mysql",
    "express",
    "nodejs",
    "react",
    "fullstack",
    "rest"
  ],
  "author": "joaomouraosa",
  "license": "MIT"
}
